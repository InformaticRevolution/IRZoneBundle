<div class="page-header">
    <h1>{{ 'ir_zone.admin.country.new.title'|trans }}</h1>
</div>

<form action="{{ path('ir_zone_admin_country_new') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRZoneBundle:Admin/Country:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'ir_zone.admin.country.new.save'|trans }}</button>
    </div>         
</form>