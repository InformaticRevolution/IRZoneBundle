<div class="page-header">
    <h1>{{ 'ir_zone.admin.region.new.title'|trans }}</h1>
</div>

<form action="{{ path('ir_zone_admin_region_new', {'countryId': region.country.id}) }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRZoneBundle:Admin/Region:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'ir_zone.admin.region.new.save'|trans }}</button>
    </div>         
</form>